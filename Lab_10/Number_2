#include <iostream>

int main() {
    const int m = 6;
    const int n = 4;
    int a[m][n];

    for (int i = 0; i < m; ++i) {
        for (int j = 0; j < n; ++j) {
            std::cin >> a[i][j];
        }
    }

    for (int j = 0; j < n; ++j) {
        int evenCount = 0;
        for (int i = 0; i < m; ++i) {
            if (a[i][j] % 2 == 0) evenCount++;
        }
        std::cout << "Column " << j << ": " << evenCount << " even numbers\n";
    }

    return 0;
}
